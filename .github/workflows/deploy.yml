name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - run: pip install mkdocs-material

      - name: Prepare MkDocs Content
        run: |
          mkdir -p docs_temp/sets docs_temp/bool docs_temp/logic-gates docs_temp/logic-operators docs_temp/rules
          cp README.md docs_temp/index.md
          rsync -a sets/ docs_temp/sets/
          rsync -a bool/ docs_temp/bool/
          rsync -a logic-gates/ docs_temp/logic-gates/
          rsync -a logic-operators/ docs_temp/logic-operators/
          rsync -a rules/ docs_temp/rules/

      - run: mkdocs gh-deploy --force
